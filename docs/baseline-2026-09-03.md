# 验证服务器基线快照(冻结于本次"喀什深度开发"开始前)

> **冻结时间**:2026-09-03(本快照对应 commit `b12be09` 部署后的稳定运行状态)
> **用途**:开始对 Agent 进行深度开发期间,验证服务器保持此基线不动,作为开发完成后对比与回归的参考。
> **原则**:本基线期间,本工作助手**不会**对服务器执行任何变更/拉取/编排;后续仅在你明确说"恢复部署"时,才按本地 push → CI → 服务器拉取的流程收敛。

## 1. 仓库最近 5 个 commit(本基线包含)
```
b12be09 feat(agent): 事件级幂等——消费前置 seen_event_ids 过滤,重复 XADD/丢 ack 重投的已成功事件直接 ack 跳过,不重复分析;新增 events.dedup/unique 指标
8b22cb3 fix(salt): elastic-agent 容器以烘焙启动脚本为 ENTRYPOINT,绕过官方 docker-entrypoint
dd281e8 fix(images): elastic-agent 启动脚本防御 docker-entrypoint 的 delay 半成品 fleet.enc(<500B 视为无效重 enroll)
056526c fix(salt): configs.sls 空列表时补 ensure-env-dir 保底 state,避免编排 salt.state 空跑判失败
6e58a2 fix(images): elastic-agent 基础镜像默认非 root,COPY --chmod=755 + USER root 段内做 fleet 配置复制
```
> 上述 5 个 commit 已全部 `git push origin main`;服务器当前即运行此基线。

## 2. 验证服务器容器与镜像(只读快照)

### 12 个运行中容器
| 容器 | 状态 | 镜像 tag | 镜像 ID |
|---|---|---|---|
| nss-ndr-salt-master-api | Up 36h | salt-master-api:latest | c65baf50a8b4 |
| nss-ndr-elasticsearch | Up 35h | elasticsearch:9.5.2 | f060c4664039 |
| nss-ndr-redis | Up 35h | redis-databus:8.10.1 | 28a5dcd2a7ab |
| nss-ndr-logstash | Up 35h | logstash-databus:9.5.2 | 8998a1e3387d |
| nss-ndr-zeek | Up 35h | zeek-databus:8.2.2 | e95c1bcdb95c |
| nss-ndr-llm-server | Up 25h (healthy) | llm-server:latest | 22d223e49317 |
| nss-ndr-kibana | Up 12h | kibana:9.5.2 | cc915b2106ed |
| nss-ndr-salt-minion | Up 12h | salt-minion:latest | 3c9fa77f34ad |
| nss-ndr-fleet-server | Up 12h (healthy) | fleet-server:9.5.2 | acb420c610e3 |
| nss-ndr-elastic-agent | Up 11h (healthy) | elastic-agent-zeek:9.5.2 | c758822fa5e9 |
| nss-vault | Up 11h | hashicorp/vault:latest | dfc8ef82a70a |
| nss-ndr-agent | Up 10h (healthy) | **agent:latest bcc761f6ce59** |

> 关键镜像:agent `:latest` = `bcc761f6ce59`(commit `b12be09` 构建)

## 3. Redis 队列状态
```
Stream analysis:events: length 17424, entries-read 17421, lag 3, pending 3(消费健康)
Stream analysis:events:dlq: 0(无毒丸)
```

## 4. LLM 服务关键参数
| 参数 | 值 |
|---|---|
| LLM_CONTEXT_SIZE | 6144 |
| LLM_CONTEXT_RATIO | 1.0 |
| LLM_PARALLEL | 2(slot 3072) |
| LLM_BATCH_SIZE / U_BATCH | 1024 / 256 |
| LLM_THREADS_RATIO | 0.75(threads 由 entrypoint 算) |
| LLM_CACHE_TYPE_K/V | q8_0 / q8_0 |
| LLM_EXTRA_ARGS | `--chat-template-kwargs {"enable_thinking":false}` |
| LLM_MODEL | /models/Qwen3-0.6B-Q8_0.gguf |
| cpuset_cpus | 0-6(shares 768) |

## 5. Agent 配置(烘焙进镜像,运行时无法改)
- batch_size=50, max_tool_calls=2, max_output_tokens=384, model_timeout=45.0, dry_run=False
- providers(edge/cloud) `json_schema=False`(打通工具调用路径)

## 6. 已落地/已废止项
- `.env-credentials` 已删除;密码唯一来源 = Vault(`nss-vault-data`)
- `/root/.vault-admin` 保留(600,Vault 管理凭据;unseal 用)
- 临时文件、匿名卷、AppleDouble、`.bak` 已全部清理
- 所有容器配置/脚本烘焙进镜像,无宿主文件挂载(agent/kibana/fleet/elastic-agent)

## 7. 开发期间约束(给本地/Kashgar 端参考)
- 此期间服务器**不会**自动拉取新镜像或执行 salt 编排,即使 origin 有新 commit。
- 本地可继续修改并 push(只触发 CI 重新构建镜像,服务器不拉取)。
- 当你完成开发后告知"恢复部署",再按标准流程:本地 push → 服务器拉取对应 sha → 编排收敛 → 验证。

## 8. 复现基线的最小验证命令
```bash
ssh <user>@172.16.196.79 "bash /tmp/snap.sh"  # 见 git 历史中的快照脚本
# 或手测:docker ps / docker image ls / 队列 /vault 状态 等
```
