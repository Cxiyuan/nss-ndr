title: 恶意 JA3 指纹确认（ET JA3 线索 + Zeek TLS 会话）
id: 11111111-0005-4000-8000-000000000005
status: stable
level: medium
backend: auto
logsource:
  product: suricata
  category: network
description: JA3 恶意指纹线索命中后，联动同 community_id 的 Zeek TLS 会话确认真实 TLS 握手发生，过滤指纹匹配噪音。
correlation:
  clue:
    logsource:
      product: suricata
    detection:
      selection:
        rule.name|contains: "JA3"
      condition: selection
  confirm:
    logsource:
      product: zeek
      category: tls
    detection:
      selection:
        tls.version|startswith: TLS
      condition: selection
  group_by: network.community_id
  timespan: 5m
  required: both
  fallback: clue_only
  confidence: medium
