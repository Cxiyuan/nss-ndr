title: 钓鱼站点访问确认（ET PHISHING 线索 + Zeek HTTP 提交）
id: 11111111-0006-4000-8000-000000000006
status: stable
level: medium
backend: auto
logsource:
  product: suricata
  category: network
description: 钓鱼线索命中后，联动同 community_id 的 Zeek HTTP 记录确认用户实际访问钓鱼页并提交表单（POST + 200），过滤纯黑名单域名误报。
correlation:
  clue:
    logsource:
      product: suricata
    detection:
      selection:
        rule.name|contains: "ET PHISHING"
      condition: selection
  confirm:
    logsource:
      product: zeek
      category: http
    detection:
      selection:
        http.request.method: POST
        http.response.status_code: 200
      condition: selection
  group_by: network.community_id
  timespan: 5m
  required: both
  fallback: clue_only
  confidence: medium
