input {
  redis {
    host => "${REDIS_HOST:-nss-redis}"
    port => 6379
    password => "${REDIS_PASSWORD:-nss-ndr}"
    key => "logstash:unparsed"
    data_type => "list"
    batch_count => 125
    threads => 2
  }
}
