apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: nss-filebeat
  namespace: nss-ndr
  labels:
    app: nss-filebeat
spec:
  revisionHistoryLimit: 2
  selector:
    matchLabels:
      app: nss-filebeat
  template:
    metadata:
      labels:
        app: nss-filebeat
    spec:
      tolerations:
        - operator: Exists
      containers:
        - name: filebeat
          image: ghcr.io/cxiyuan/nss-ndr/nss-ndr-filebeat
          imagePullPolicy: IfNotPresent
          env:
            - name: ES_HOST
              value: "http://nss-elasticsearch:9200"
            - name: ES_USERNAME
              value: "filebeat"
            - name: ES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: nss-ndr-secrets
                  key: filebeat-password
          volumeMounts:
            - name: config
              mountPath: /usr/share/filebeat/filebeat.yml
              subPath: filebeat.yml
              readOnly: true
            - name: nsm
              mountPath: /nsm
              readOnly: true
            - name: data
              mountPath: /usr/share/filebeat/data
      volumes:
        - name: config
          configMap:
            name: nss-ndr-config
        - name: nsm
          persistentVolumeClaim:
            claimName: nss-ndr-nsm
        - name: data
          emptyDir: {}
