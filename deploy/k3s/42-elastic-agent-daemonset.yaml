apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: nss-elastic-agent
  namespace: nss-ndr
  labels:
    app: nss-elastic-agent
spec:
  revisionHistoryLimit: 2
  selector:
    matchLabels:
      app: nss-elastic-agent
  template:
    metadata:
      labels:
        app: nss-elastic-agent
    spec:
      tolerations:
        - operator: Exists
      containers:
        - name: elastic-agent
          # Fleet 托管模式（对齐 SO）：配置/集成由 Fleet 下发。
          # 容器模式必需 FLEET_ENROLL=1（否则以 standalone 跑默认配置不注册）、
          # FLEET_URL + FLEET_ENROLLMENT_TOKEN + FLEET_CA（信任 Fleet Server 证书）。
          # 注意：不能把卷挂到 /usr/share/elastic-agent/data（会遮蔽镜像内二进制软链）。
          image: ghcr.io/cxiyuan/nss-ndr/nss-ndr-elastic-agent
          imagePullPolicy: IfNotPresent
          env:
            - name: NSS_SENSORNAME
              valueFrom:
                configMapKeyRef:
                  name: nss-ndr-config
                  key: sensor_id
            - name: FLEET_ENROLL
              value: "1"
            - name: FLEET_URL
              value: "https://nss-fleet-server:8220"
            - name: FLEET_ENROLLMENT_TOKEN
              valueFrom:
                secretKeyRef:
                  name: nss-fleet-enrollment
                  key: enrollment_token
            - name: FLEET_CA
              value: "/etc/elastic-agent/certs/ca.crt"
          volumeMounts:
            - name: nsm
              mountPath: /nsm
              readOnly: true
            - name: certs
              mountPath: /etc/elastic-agent/certs
              readOnly: true
          resources:
            requests:
              memory: 256Mi
            limits:
              memory: 1Gi
      volumes:
        - name: nsm
          persistentVolumeClaim:
            claimName: nss-ndr-nsm
        - name: certs
          secret:
            secretName: nss-ndr-certs
