# NSS-NDR Kibana 配置

server.host: "0.0.0.0"
server.port: 5601

elasticsearch.hosts: ["http://nss-elasticsearch:9200"]
elasticsearch.ssl.verificationMode: none
# 认证由容器 env（ELASTICSEARCH_USERNAME/ELASTICSEARCH_PASSWORD）注入

logging.root.level: info
